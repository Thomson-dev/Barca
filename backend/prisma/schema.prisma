generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// Models for each module (User, Team, Player, Match, News, Video, Post,
// Comment, Prediction, Ticket, Product, Notification) go here.
